class ColumnTypes:
    """Defines constants for all the column types."""

    STRING = 'string'
    INTEGER = 'integer'
    DECIMAL = 'decimal'
    ENUM = 'enum'
    ALL = [STRING, INTEGER, DECIMAL, ENUM]
